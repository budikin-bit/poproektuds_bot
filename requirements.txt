# Прямые зависимости, зафиксированы по версиям с работающего сервера
# (pip freeze от 02.07.2026, сборка прошла приёмку этапов 1–2).
# Обновлять осознанно: поднять версию -> прогнать полный цикл
# опрос->анализ->PDF -> закоммитить новый пин.

maxapi==1.2.1
anthropic==0.115.1
httpx==0.28.1
aiohttp==3.14.1
fpdf2==2.8.7
cloakllm==0.11.5
spacy==3.8.14
python-dotenv==1.2.2
click==8.4.2
pytz==2026.2

# Примечание: русская spacy-модель (ru_core_news_sm) НЕ требуется —
# cloakllm маскирует русские ФИО собственным механизмом (подтверждено
# самопроверкой в pii.warmup: «тестовое ФИО замаскировано — OK»).
# Если после обновления cloakllm самопроверка на старте выдаст WARNING —
# начать разбирательство с NER-моделей.
